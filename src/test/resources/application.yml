server:
  servlet:
    context-path: /motor-mingle

spring:
  config:
    activate:
      on-profile: gcp

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:motormingletest
    username: motormingle
    password: motormingle

  liquibase:
    parameters:
      changeLogFile: src/main/resources/db/changelog/db.changelog-master.yaml
